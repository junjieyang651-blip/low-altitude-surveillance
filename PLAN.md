# 低空目标多元融合监视系统 — 详细开发计划

> 目标：国一水准 | 工程可落地 | 避免AI模板化

---

## 一、总体技术架构

```
┌─────────────────────────────────────────────────────────────┐
│                    应用层（Web态势平台）                       │
│  ┌──────────┐ ┌──────────┐ ┌──────────┐ ┌──────────────┐   │
│  │ 态势大屏  │ │ 冲突预警  │ │ 异常分析  │ │ 流量预测报告  │   │
│  └──────────┘ └──────────┘ └──────────┘ └──────────────┘   │
├─────────────────────────────────────────────────────────────┤
│                    算法引擎层                                │
│  ┌──────────────┐ ┌──────────────┐ ┌────────────────────┐  │
│  │ 时空对齐引擎  │ │ 融合关联引擎  │ │ 异常与冲突检测引擎  │  │
│  └──────────────┘ └──────────────┘ └────────────────────┘  │
├─────────────────────────────────────────────────────────────┤
│                    数据层（4源异构数据）                        │
│       ADS-B  +  Remote ID  +  雷达  +  频谱                  │
└─────────────────────────────────────────────────────────────┘
```

---

## 二、模块拆分与里程碑

### Phase 1：数据底座与时空一致性（Day 1-2）
**核心任务**：将4类异构数据统一到同一时空基准下，解决传感器差异。

- **时间对齐**：统一时间戳（rev_time 转秒级相对时间），处理各源采样率差异
- **坐标转换**：
  - 雷达极坐标（斜距/方位角/俯仰角）→ 经纬度高程
  - 建立统一ENU（东北天）局部坐标系便于距离计算
- **数据质量清洗**：野值剔除、插值补全、时间戳异常修正

**创新点**：提出"**动态采样率自适应对齐算法**"——根据各源数据密度动态选择插值策略（线性/样条），而非简单重采样，保留原始信息密度。

---

### Phase 2：多源异构融合关联（Day 2-3）
**核心任务**：判断不同传感器看到的是不是同一个目标，并融合成统一航迹。

- **单源航迹生成**：将雷达点迹按 track_number 聚合成航迹；ADS-B/Remote ID 按 icao/uas_id 聚合
- **异源关联**：
  - 构建"**自适应时空窗多特征相似度度量**"
  - 特征：空间距离、速度矢量夹角、高度差、航向差
  - 时间窗根据目标速度动态调整（高速目标窗口小，低速窗口大）
  - 采用最近邻 + 马氏距离阈值判决，工程可解释
- **航迹融合**：加权最小二乘融合，权重依据各源定位精度先验设定

**创新点**：
1. **速度自适应时间窗**：传统固定窗口在高速场景下容易错关联，根据 speed 动态调节 Δt = k / v，体现工程细节。
2. **多置信度分级关联**：将关联结果分为"确定关联""疑似关联""待确认"三级，后续用航迹连续性进行逻辑后校验，降低虚警。

---

### Phase 3：目标分类与异常检测（Day 3-4）
**核心任务**：区分无人机/通航飞机，识别异常飞行行为。

- **目标分类**（轻量可解释）：
  - 规则引擎：高度分层 + 速度范围 + 机动特征
  - 频谱源直接提供 uav_model_text，可作为标签进行半监督验证
  - 输出：通航飞机 / 消费级无人机 / 行业级无人机 / 未知非合作目标
- **异常检测**（物理驱动+统计）：
  - **动力学约束异常**：根据机型物理极限（如最大爬升率、转弯率）检测不可能运动
  - **统计异常**：基于航迹的局部离群因子（LOF）检测偏离常规航线行为
  - **ADS-B/Remote ID 缺失异常**：合作目标信号丢失（可能为恶意关闭）

**创新点**：
- **双引擎异常检测**：规则引擎负责"物理不可能"硬异常（100%置信度），统计引擎负责"行为偏离"软异常，两者互补，避免纯深度学习的黑盒不可解释问题。
- **信号完整性监控**：实时监测合作目标（ADS-B/Remote ID）是否异常消失，作为潜在威胁指标。

---

### Phase 4：冲突检测与风险预警（Day 4-5）
**核心任务**：预测目标间潜在碰撞，输出风险等级。

- **改进型人工势场法**：
  - 将每个目标视为势场源，构建排斥势场
  - 引入相对速度修正：高速接近时势场增强，同向并行时势场减弱
  - 计算势能阈值触发预警
- **DCPA/TCPA 经典模型**：
  - DCPA（最近会遇距离）、TCPA（到达最近点时间）
  - 设置分级阈值：注意 / 警告 / 紧急
- **地理围栏入侵检测**：对关键区域（如机场净空区）设置电子围栏

**创新点**：
- **速度方向修正势场**：传统势场法只考虑距离，不考虑相对运动态势。引入速度夹角因子修正势场强度，使预警更符合航空实际。
- **分级预警+冲突解脱建议**：不仅报风险，还基于势场梯度给出建议航向调整角，体现"平台化"价值。

---

### Phase 5：流量分析与可视化（Day 5-6）
**核心任务**：宏观态势感知与系统展示。

- **流量分析**：
  - 时域流量曲线（架次/小时）
  - 空域热力图（栅格化密度统计）
  - 航线聚类（DBSCAN提取主要航线走廊）
- **Web态势平台**：
  - 后端：Flask 提供 RESTful API
  - 前端：Leaflet.js 地图 + ECharts 统计图表
  - 功能：实时航迹显示、历史回放、冲突告警弹窗、流量报表

---

## 三、国一竞争力分析

| 维度 | 常见做法（易雷同） | 本方案差异化 |
|------|------------------|-------------|
| 融合关联 | 简单最近邻或端到端深度学习 | **速度自适应窗口+分级关联+后校验**，工程细节扎实 |
| 异常检测 | 纯 LSTM/Transformer 黑盒 | **物理约束+统计双引擎**，可解释、可落地 |
| 冲突检测 | 简单距离阈值 | **改进势场法+DCPA/TCPA**，有物理意义且输出解脱建议 |
| 成果形式 | 只有论文和结果表格 | **完整可运行的Web平台**，评委可交互体验 |
| 创新性 | 堆砌深度学习模型 | **从工程问题出发**（时间窗该多大？势场如何修正？），问题驱动 |

---

## 四、代码目录规划

```
├── core/
│   ├── __init__.py
│   ├── time_align.py          # 时间对齐与坐标转换
│   ├── association.py         # 异源航迹关联算法
│   ├── fusion.py              # 航迹融合（加权最小二乘）
│   ├── classification.py      # 目标分类引擎
│   ├── anomaly_detection.py   # 异常检测双引擎
│   ├── conflict_detection.py  # 冲突检测（势场+DCPA）
│   └── flow_analysis.py       # 流量统计与航线聚类
├── utils/
│   ├── __init__.py
│   ├── coords.py              # 坐标转换工具（WGS84/ENU/极坐标）
│   ├── io_utils.py            # 数据读取与预处理
│   └── constants.py           # 物理常数与阈值配置
├── web/
│   ├── app.py                 # Flask主应用
│   ├── static/
│   └── templates/
├── data/                      # 原始数据（勿提交）
├── output/                    # 中间结果与报表
├── notebooks/                 # 探索性分析
└── main.py                    # 一键运行流水线
```

---

## 五、下一步行动

按 Phase 1 → Phase 2 → Phase 3 → Phase 4 → Phase 5 顺序，逐模块编写代码、验证数据、迭代优化。每个Phase结束都应有可运行的中间产物和可视化结果。
